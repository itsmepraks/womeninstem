import BookReader from '@/components/book/BookReader';

/**
 * Demo page - Shows the book reader
 */
export default function DemoPage() {
  return (
    <div className="min-h-screen">
      <BookReader />
    </div>
  );
}